#include <stdio.h>
int main ()
{
    long long int t,a,b,i;
    scanf ("%lld",&t);
    for (i=1; i<=t; i++)
    {
        scanf ("%lld%lld",&a,&b);
        printf ("YES\n");
    }

    return 0;
}
